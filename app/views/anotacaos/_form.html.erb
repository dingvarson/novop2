<%= form_for(@anotacao) do |f| %>
  <% if @anotacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anotacao.errors.count, "error") %> prohibited this anotacao from being saved:</h2>

      <ul>
      <% @anotacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
